/**
 * Здесь ключ вычисляется из кода ошибки, а не пишется литералом,
 * поэтому переводчик описан свободной сигнатурой. На вызывающей стороне
 * типизация сообщений сохраняется.
 */
export type LooseTranslate = (key: string, values?: Record<string, string | number>) => string;
