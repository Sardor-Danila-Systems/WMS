/**
 * Имя cookie сессии вынесено отдельно от `session.ts`: этот модуль импортирует
 * proxy, который выполняется на каждый запрос и не должен тянуть за собой
 * подключение к базе данных.
 */
export const SESSION_COOKIE = "wms_session";
