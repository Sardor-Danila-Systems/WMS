export interface Supplier {
  id: string;
  name: string;
  contact: string;
}

export interface Worker {
  id: string;
  name: string;
  phone: string;
  position: string;
}

export interface Foreman {
  id: string;
  name: string;
  phone: string;
  brigade: string;
}
